import { styled, Typography } from '@mui/material/';

export const TitleComponent = styled(Typography)(({ theme }) => ({
    color: '#000000DE',
    fontFamily: 'Roboto',
    fontSize: '24px',
    fontWeight: 'bold',
}));